---
layout: default.njk
title: Disruption 2020 // Curtin Grad Show
pagination:
    data: majorsComputed
    size: 1
    alias: major
permalink: "major/{{ major.title | slug }}/"
description: Disruption 2020 is the final year showcase of Curtin University Design & Architecture graduates. We're fluid, we're flexible, we bend and push until a design is resolved. When they zig, we zag. We're accustomed to disruption, and successful in the face of upheaval. Come meet us, and see what our warped minds are capable of.

--- 

<section data-title="{{ major.title }} // Disruption 2020 // Curtin Grad Show" class="banner page-padding py-6">
  <div class="banner-text">
    {% if major.id === '0' %}
      <p class="major-intro transition-fadeout">Design & Architecture Graduates</p>
    {% else %}
      <p class="major-intro transition-fadeout">Graduates From The Major Of</p>
    {% endif %}
    <h1 class="title major-title is-size-1 is-size-4-mobile" data-major-id="{{ major.id }}" >{{ major.title }}</h1>
  </div>
  {% if major.color %}
    <div class="entry-fadein-delay-2">
      <img src="{{ major.color }}" class="banner-img" alt="">
    </div>
  {% endif %}
</section>
{% if major.id !== '0' %}
  <section class="profile-listing-showreel-container">
      <iframe 
        width="1200" 
        height="700" 
        src="https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ?rel=0&color=white&loop=1&playsinline=1" 
        frameborder="0" 
        allowfullscreen
      ></iframe>
  </section>
{% endif %}
<section class="py-6 page-padding mx-auto min-h-full">
  <div class="search-bar-section mb-6">
    <div class="search-bar-wrapper">
      <div class="control has-icons-left">
          <input class="search-bar" type="text" placeholder="Search">
          <span class="icon is-left search-icon">
            <i class="fas fa-search"></i>
          </span>
      </div>
      <div class="select">
        <select id="sortby">
          <option value="none">None</option>
          <option value="firstaz">First Name A-Z</option>
          <option value="firstza">First Name Z-A</option>
          <option value="lastaz">Last Name A-Z</option>
          <option value="lastza">Last Name Z-A</option>
        </select>
      </div>
      <label class="check">
        <input type="checkbox" id="ispref" checked="true">
        <span>Preferred Names</span>
      </label>
    </div>
  </div>
  <div class="student-profiles student-profile-container">
  </div>
</section>

